<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white">
       <div class="flex w-full justify-end">

        <a class="bg-red-500 text-sm inline-block mb-5 rounded-md px-3 py-2" href="/logout"> Logout</a>
       </div>
      
        
        <h5 class="mb-5"> Edit your Post</h5>
        <form method="post" action="/update/<%= post._id %>">
            <textarea name="content" placeholder="Whats on your mind?" class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"> <%= post.content  %> </textarea>
            <input class="px-3 w-40 py-2 bg-yellow-500 text-black mt-2 text-sm rounded-md block" type="submit" value="Update New Post">
        </form>

        
       
    </div>
</body>
</html>