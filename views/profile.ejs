<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white">
       <div class="flex w-full justify-end">

        <a class="bg-red-500 text-sm inline-block mb-5 rounded-md px-3 py-2" href="/logout"> Logout</a>
       </div> 
       <div class="flex items-start gap-3">
        <div class="w-10 h-10 rounded-md ">
            <img class="w-full object-cover h-full"  src="/images/uploads/<%= user.profilepic %>" alt="working">
        </div>
        <h3 class="tracking-tighter text-3xl"><span class="font-light"> Hello, </span> <%= user.name %> </h3>
        </div>
        <h5 class="mb-5 mt-3"> You can Create  a New Post</h5>
        <form method="post" action="/post">
            <textarea name="content" placeholder="Whats on your mind?" class="w-1/3 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md"></textarea>
            <input class="px-3 w-40 py-2 bg-blue-500 mt-2 text-sm rounded-md block" type="submit" value="Create New Post">
        </form>

        <div class=" mt-20 posts">
            <h3 class="text-zinc-400"> Your Posts</h3>
            <div class="postcontainer mt-5 ">
           

            <% user.posts.reverse().forEach(function(post){ %>
                <div class="post w-1/3 mb-4 p-4 bg-zinc-800 rounded-md border-[1px] border-zinc-700"> 
                    <h4 class="text-blue-500 mb-2"> @ <%= user.username %></h4>
                    <p class="text-sm tracking-tight "> <%= post.content %></p>
                   
                   <small class="mt-2 inline-block"> <%= post.likes.length %> likes</small>
                    <div class="btn flex gap-4 mt-1">
                      <a href="/like/<%= post._id%>" class="text-blue-500"><%= post.likes.indexOf(user._id)=== -1 ? "Like" : "Unlike" %></a>
                      <a href="/edit/<%= post._id%>" class="text-zinc-400"> Edit</a>
                    </div>
                  </div>
          <%  }) %>
        </div>
        </div>
       
    </div>
</body>
</html>